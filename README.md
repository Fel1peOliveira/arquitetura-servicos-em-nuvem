# arquitetura-servicos-em-nuvem
Workflow n8n de um microsserviço de Catálogo/Cardápio com autenticação por API key, consulta de estoque com fallback via Redis e observabilidade (health check, logs e métricas).
